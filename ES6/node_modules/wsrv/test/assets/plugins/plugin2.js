'use strict';

exports.register = function (server, options, next) {
    server.route({
        method: 'GET',
        path: '/plugin2',
        handler: function (request, reply) {
            reply('plugin 2 test passed');
        }
    });

    next();
};

exports.register.attributes = {
    name: 'test-plugin-2',
    version: '1.0.0'
};
