image: node:latest

cache:
  paths:
  - node_modules/
  #- .yarn-cache

test:
  script:
   #- yarn install --cache-folder .yarn-cache
   - yarn install
   - yarn test
  after_script:
   - bash <(curl -s https://codecov.io/bash)

pages:
  stage: deploy
  script:
  - echo 'Nothing to do...'
  artifacts:
    paths:
    - public
  only:
  - master

